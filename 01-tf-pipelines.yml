trigger:
 branches:
   exclude:
     - main

pool: 1agpool

stages:
  - stage: "TerraformInit"
    displayName: "Terraform init"
    jobs:
      - job: "InitPlan"
        displayName: "Terraform Init & Plan"
        steps:
        - task: TerraformTask@5
          inputs:
            provider: 'azurerm'
            command: 'init'
            backendServiceArm: 'pay-as-you-go-dev(67bc5246-befd-4265-a0e6-6b7f6196364f)'
            backendAzureRmStorageAccountName: 'mystoragefortfstatefile'
            backendAzureRmContainerName: 'mycontainerfortfstate'
            backendAzureRmKey: 'project01-tfstate'

        - task: TerraformTask@5
          inputs:
            provider: 'azurerm'
            command: 'plan'

  - stage: "TerraformApply"
    displayName: "Terraform Apply Step"
    jobs:
    - job: "tfapply"
      displayName: "Terraform Apply Step"
      steps:
      - task: TerraformTask@5
        inputs:
          provider: 'azurerm'
          command: 'apply'

